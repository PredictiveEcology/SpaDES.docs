% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{installModulePkgs}
\alias{installModulePkgs}
\title{Install the packages a manual's modules need}
\usage{
installModulePkgs(
  modulePath = "modules",
  modules = NULL,
  dependencies = TRUE,
  install = TRUE
)
}
\arguments{
\item{modulePath}{directory holding the module directories.}

\item{modules}{names of the modules to resolve. Defaults to every directory
under \code{modulePath}.}

\item{dependencies}{whether to expand each module package's own dependencies
with \code{\link[Require:pkgDep2]{Require::pkgDep2()}} before installing. \code{FALSE} installs only the
packages the modules name.}

\item{install}{whether to install. \code{FALSE} resolves the package list and
returns it without installing anything, which is useful for seeing what a
manual's modules ask for.}
}
\value{
the packages, invisibly.
}
\description{
The modules a manual documents declare their own \code{reqdPkgs}, which the manual
must install before it can knit their chapters. Call this after the manual's
own dependencies are in place -- typically right after
\code{remotes::install_deps()}, which is what installs this package.
}
\details{
The package list is deliberately assigned before being installed rather than
piped. \code{\link[Require:Install]{Require::Install()}} names its first parameter \code{packages} and calls
\code{\link[=substitute]{substitute()}} on it, so a piped expression resolves to the literal string
\code{"packages"} and the installer is asked for a package by that name.
\code{\link[Require:pkgDep2]{Require::pkgDep2()}} also returns a named list, which \code{Install()} wants
flattened.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{stageFigure}
\alias{stageFigure}
\title{Put a module figure where the rendered document can find it}
\usage{
stageFigure(
  path,
  stageDir = knitr::opts_knit$get("SpaDES.docs.stageDir"),
  outputDir = knitr::opts_knit$get("output.dir")
)
}
\arguments{
\item{path}{figure path(s) relative to the module directory, i.e. what the
module uses when it renders on its own -- for example
\code{"figures/schematic.png"}. A vector is staged element by element.}

\item{stageDir}{directory the staged chapter's figures go in, relative to the
book root. The default reads what \code{\link[=prepManualRmds]{prepManualRmds()}} set for the chapter; pass
it explicitly only when testing.}

\item{outputDir}{directory the document is being rendered in, i.e. the book
root. The default asks knitr; pass it explicitly only when testing.}
}
\value{
the path(s) to use in the reference: \code{path} unchanged when the module
renders on its own, otherwise the relative path(s) to the staged copies.
}
\description{
A module chapter is rendered from wherever the \emph{document} lives: the module's
own directory when the module renders on its own, and the book root once
\code{\link[=prepManualRmds]{prepManualRmds()}} has staged it into a manual. A relative path written for
one of those is wrong in the other, and it is the chapter that moves, not the
figure.
}
\details{
Writing an absolute path instead -- via \code{normPath()}, say -- makes the render
succeed and the \emph{publication} fail. The path is correct only on the machine
that built the book, so the deployed site serves a dead link from every
chapter, and the build stays green while it happens.

When the chapter has been staged, this copies \code{path} into a directory of the
module's own beside the staged chapter, and returns the relative path to that
copy, so the file travels with the book into the published output. The
directory is per module because modules reuse conventional names: every one
writes \code{figures/moduleVersionBadge.png}, and a shared directory would show
each chapter the badge of whichever module was knitted last.

When the module renders on its own, \code{path} is returned unchanged and nothing
is copied: its relative paths already resolve from its own directory.
\code{\link[=prepManualRmds]{prepManualRmds()}} marks a staged chapter by setting the knitr option
\code{SpaDES.docs.stageDir} in its setup chunk; that option's absence is what
"rendering on its own" means here.

A figure fetched at render time is handled the same way: write it into the
module's own \verb{figures/} first, as usual, then pass it through.
}
\examples{
\dontrun{
## in a module chapter, instead of normPath():
knitr::include_graphics(stageFigure("figures/schematic.png"))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{includeFigure}
\alias{includeFigure}
\title{Include a module figure from a chunk}
\usage{
includeFigure(path, ...)
}
\arguments{
\item{path}{figure path(s) relative to the module directory, i.e. what the
module uses when it renders on its own -- for example
\code{"figures/schematic.png"}. A vector is staged element by element.}

\item{...}{further arguments to \code{\link[knitr:include_graphics]{knitr::include_graphics()}}, e.g. \code{dpi}.}
}
\value{
what \code{\link[knitr:include_graphics]{knitr::include_graphics()}} returns.
}
\description{
The chunk form of \code{\link[=stageFigure]{stageFigure()}}: stages \code{path} and hands the result to
\code{\link[knitr:include_graphics]{knitr::include_graphics()}}, so a module chapter includes the same figure
whether it renders on its own or staged into a manual.
}
\details{
\code{knitr::include_graphics(stageFigure(path))} does not work from a staged
chapter. \code{include_graphics()} checks that the file exists relative to the
working directory, which for a staged chapter is the module's (knitr's
\code{root.dir}), while the path \code{\link[=stageFigure]{stageFigure()}} returns is relative to the book
root, where the rendered document resolves it. So this turns that check off
-- \code{\link[=stageFigure]{stageFigure()}} has already made the same check, against the right
directory -- and passes everything else through.

Use \code{\link[=stageFigure]{stageFigure()}} directly only to write the markdown yourself, e.g. for a
linked badge, \code{cat(paste0("[![alt](", stageFigure(p), ")](", url, ")"))}.
}
\examples{
\dontrun{
## in a module chapter's figure chunk, instead of
## knitr::include_graphics(normPath("figures/schematic.png")):
includeFigure("figures/schematic.png")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publishing.R
\name{publishManualArchive}
\alias{publishManualArchive}
\title{Publish a manual's archived releases alongside the site}
\usage{
publishManualArchive(
  archiveDir = file.path("archive", "pdf"),
  docsDir,
  manualName,
  subdir = "archive"
)
}
\arguments{
\item{archiveDir}{directory holding the archived PDFs, usually tracked in the
repository.}

\item{docsDir}{the rendered book directory, i.e. \verb{_bookdown.yml}'s
\code{output_dir}.}

\item{manualName}{name of the manual, used in the index page's headings and
link text.}

\item{subdir}{directory within \code{docsDir} to publish into.}
}
\value{
path to the index page, invisibly, or \code{NULL} if there was nothing to
publish.
}
\description{
A manual that is released keeps a PDF of each release. Those PDFs belong in
version control rather than only on the published branch: that branch is
rebuilt by every deploy, and an old PDF cannot be regenerated from current
sources. This copies them into the rendered book directory so they deploy with
the site, and writes an index page listing them.
}
\details{
The index is built from the files actually present, not from a list kept by
hand, so it cannot drift. Versions are read from the file names, which are
what \code{\link[=archiveManualPDF]{archiveManualPDF()}} writes: \verb{<prefix>-v<version>.pdf}. Anything not
matching that shape is copied but left out of the ordering.
}

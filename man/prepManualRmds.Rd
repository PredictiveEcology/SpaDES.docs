% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LandR-related.R
\name{prepManualRmds}
\alias{prepManualRmds}
\title{Prepare module .Rmd to render book}
\usage{
prepManualRmds(
  modulePath,
  rebuildCache = FALSE,
  ignoreModules = NULL,
  bookdownYML = "_bookdown.yml",
  stagingPath = "_manual_rmds"
)
}
\arguments{
\item{modulePath}{modules' folder directory, specified exactly as in \verb{_bookdown.yml}.
For instance, if in \verb{_bookdown.yml} the list of module \code{.Rmd} files is provided as
\code{-  modules/XXX.Rmd}, \verb{-  ~/modules/XXX.Rmd}, or \code{-  ../X/modules/XXX.Rmd}, then
\code{modulePath} must be either \code{modules}, \verb{~/modules} or \code{../X/modules} respectively.
A \code{/} may be appended at the of \code{modulePath} (e.g. \verb{~/modules/}).
Note that all modules must be in the same directory.}

\item{rebuildCache}{should cached chunks be re-executed?}

\item{ignoreModules}{character vector of modules to ignore.}

\item{bookdownYML}{path to the book's \verb{_bookdown.yml}, which supplies the
chapter order used when de-duplicating text references across chapters.
Read from the working directory by default.}

\item{stagingPath}{directory the generated chapters are written to, relative
to the book root. Nothing is written into the module directories, which are
git submodules in every project that uses this package -- a failed build
used to leave a \verb{<module>2.Rmd} in each one, and each module repository
carried a \code{.gitignore} line to hide it. List the chapters from here in
\verb{_bookdown.yml}, and add this directory to the book's \code{.gitignore}.}
}
\value{
file paths of the modified module \code{.Rmd} files
}
\description{
Creates modified versions of the modules' .Rmd files,
with YAML headers removed and adapted knitr setup
chunks
}

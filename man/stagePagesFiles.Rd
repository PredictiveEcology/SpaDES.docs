% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publishing.R
\name{stagePagesFiles}
\alias{stagePagesFiles}
\title{Stage the files GitHub Pages needs, inside the published directory}
\usage{
stagePagesFiles(path, cname = NULL)
}
\arguments{
\item{path}{the rendered book directory, i.e. \verb{_bookdown.yml}'s \code{output_dir}.}

\item{cname}{custom domain for the site, or \code{NULL} (the default) to write no
\code{CNAME} file. Pass the bare domain, without a scheme.}
}
\value{
\code{path}, invisibly.
}
\description{
A deploy action publishes the \emph{contents} of the rendered book directory, so
anything written to the repository root never reaches the site. Two files have
to be in the published directory itself:
}
\details{
\itemize{
\item \code{.nojekyll}, without which GitHub Pages runs the output through Jekyll and
drops the \verb{_}-prefixed directories bookdown emits;
\item \code{CNAME}, without which GitHub resets a custom domain to the default
\verb{*.github.io} address on the next deploy.
}

Writing these at the repository root appears to work for as long as an earlier
deploy left copies behind and the action deploys with \code{clean = false}. A
branch published for the first time has neither.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LandR-related.R
\name{classifyRmdLines}
\alias{classifyRmdLines}
\title{Label every line of an \code{.Rmd} by its role in the document}
\usage{
classifyRmdLines(lines)
}
\arguments{
\item{lines}{character vector of lines, as from \code{\link[=readLines]{readLines()}}.}
}
\value{
character vector the same length as \code{lines}, each element one of
\code{"chunkHeader"}, \code{"chunkBody"}, \code{"chunkEnd"} or \code{"prose"}.
}
\description{
The rewrites in \code{\link[=prepManualRmds]{prepManualRmds()}} each need to know whether a line sits
inside a fenced chunk. Deriving that separately per concern is what produced
a family of bugs: a prose mention of \code{root.dir} treated as a setting, a
\verb{## References} inside a sentence treated as a heading, and a \code{(ref:key)} use
in mid-paragraph treated as a definition and deleted.
}
\details{
Uses \code{knitr::all_patterns$md}, so a four-backtick or indented fence is
counted the way \pkg{knitr} counts it.
}
\keyword{internal}

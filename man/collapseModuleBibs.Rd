% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bibliography.R
\name{collapseModuleBibs}
\alias{collapseModuleBibs}
\title{Collapse the modules' bibliographies into one}
\usage{
collapseModuleBibs(
  modulePath = "modules",
  extraBibs = character(0),
  outFile = file.path("citations", "references.bib")
)
}
\arguments{
\item{modulePath}{directory holding the module directories, searched
recursively for \code{references_*.bib}.}

\item{extraBibs}{further \code{.bib} files to merge in, in the order given. Files
that do not exist are ignored, so a manual can list one that is only
generated on some builds.}

\item{outFile}{path to write the merged bibliography to. Its directory is
created if needed.}
}
\value{
\code{outFile}, invisibly.
}
\description{
A project manual cites references that live in each module's own
\verb{citations/references_<module>.bib}. bookdown wants a single bibliography, so
these are merged, together with any further files the manual supplies --
typically an auto-generated one for the R packages in use, and the manual's
own references.
}
\details{
Files with no entries are skipped. A module that cites nothing yet reasonably
ships a comments-only \code{.bib}, and \code{RefManageR::ReadBib()} fails on such a file
with \verb{arguments imply differing number of rows: 0, 1}, which would otherwise
take a whole manual down over one placeholder.

\code{outFile} may also appear in \code{extraBibs}: every input is read before anything
is written, so a manual can accumulate into its own bibliography as before.
}

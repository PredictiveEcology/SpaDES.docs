% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LandR-related.R
\name{prepOneModuleRmd}
\alias{prepOneModuleRmd}
\title{Turn one module's \code{.Rmd} into a book chapter}
\usage{
prepOneModuleRmd(x, rebuildCache, stagingPath)
}
\arguments{
\item{x}{path to the module's own \code{.Rmd}.}

\item{rebuildCache}{passed through from \code{\link[=prepManualRmds]{prepManualRmds()}}; the value written
into the chunk's \code{cache.rebuild} option.}

\item{stagingPath}{directory the chapter is written into. The module's own
directory is never written to; \code{root.dir} still points at it.}
}
\value{
the path of the \verb{<module>2.Rmd} written.
}
\description{
Copies \verb{<module>/<module>.Rmd} to \verb{<stagingPath>/<module>2.Rmd} and rewrites
the copy. Nothing is written into the module's own directory. The steps, in order,
because several depend on the one before:
}
\details{
\enumerate{
\item strip the YAML header, so the chapter inherits the book's output format;
\item add a chapter title if the module does not open with one;
\item find the setup chunk, or synthesize one (\code{\link[=ensureSetupChunk]{ensureSetupChunk()}});
\item force that chunk to \verb{eval = TRUE, cache = FALSE}, so it runs again
in the book even if the module set otherwise for standalone knitting;
\item point \code{root.dir} at the module directory, injecting it if absent;
\item set \code{cache.rebuild} from \code{rebuildCache}, injecting it if absent;
\item move the References heading to the end, adding one if absent, and
append the LaTeX \verb{\\\\printbibliography} command.
}

Steps 5 and 6 both inject after the chunk header, so \code{cache.rebuild} ends up
above \code{root.dir} in the generated chapter.
}
\keyword{internal}
